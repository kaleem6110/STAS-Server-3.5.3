function addLayer(layerName,description){
    try {
        window.frames["geIFrame"].addLayer(layerName,description);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function removeLayer(layerName){
    try {
        window.frames["geIFrame"].removeLayer(layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function showNetworkLinkDialog(){
    try {
        window.frames["geIFrame"].showNetworkLinkDialog();
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addPlacemark(name,description,coordinates,style,layerName){
    try {
        window.frames["geIFrame"].addPlacemark(name,description,coordinates,style,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addPolygon(name,description,coordinates,style,layerName){
    try {
        window.frames["geIFrame"].addPolygon(name,description,coordinates,style,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addPolyline(name,description,coordinates,style,layerName){
    try {
        window.frames["geIFrame"].addPolyline(name,description,coordinates,style,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addModel(coordinates,layerName,modelUrl,heading,tilt,roll,scaleX,scaleY,scaleZ){
    try {
        window.frames["geIFrame"].addModel(coordinates,layerName,modelUrl,heading,tilt,roll,scaleX,scaleY,scaleZ);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addOverlay(name,top,left,topUnit,leftUnit,iconHref,description,layerName){
    try {
        window.frames["geIFrame"].addOverlay(name,top,left,topUnit,leftUnit,iconHref,description,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function showOverlayInfo(name,description){
    try {
        window.frames["geIFrame"].showOverlayInfo(name,description);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function setSessionId(sessionId){
    try {
        window.frames["geIFrame"].setSessionId(sessionId);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function gotoLookat(latitude,longitude,altitude,range,tilt,heading){
    try {
        window.frames["geIFrame"].gotoLookat(latitude,longitude,altitude,range,tilt,heading);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function clearButtons(){
    try {
        window.frames["geIFrame"].clearButtons();
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function refreshTileLayer(){
    try {
        window.frames["geIFrame"].refreshTileLayer();
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addStyledPlacemark(name,description,coordinates,icon,scale,color,layerName){
    try {
        window.frames["geIFrame"].addStyledPlacemark(name,description,coordinates,icon,scale,color,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addStyledPolygon(name,description,coordinates,lineColor,fillColor,layerName){
    try {
        window.frames["geIFrame"].addStyledPolygon(name,description,coordinates,lineColor,fillColor,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addStyledPolyline(name,description,coordinates,lineColor,layerName){
    try {
        window.frames["geIFrame"].addStyledPolyline(name,description,coordinates,lineColor,layerName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function zoomToView(topLeftLat,topLeftLng,bottomRightLat,bottomRightLng){
    try {
        window.frames["geIFrame"].zoomToView(topLeftLat,topLeftLng,bottomRightLat,bottomRightLng);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function removeNetworkLink(name){
    try {
        window.frames["geIFrame"].removeNetworkLink(name);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addAuxObject(linkName,overlayName,overlayUrl){
    try {
        window.frames["geIFrame"].addAuxObject(linkName,overlayName,overlayUrl);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function kmlLoadingComplete(linkName){
    try {
        window.frames["geIFrame"].kmlLoadingComplete(linkName);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addLineStyle(styleId,color,width){
    try {
        window.frames["geIFrame"].addLineStyle(styleId,color,width);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addIconStyle(styleId,color,scale,heading,icon){
    try {
        window.frames["geIFrame"].addIconStyle(styleId,color,scale,heading,icon);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addListStyle(styleId,listItemType,bgColor,itemIcon){
    try {
        window.frames["geIFrame"].addListStyle(styleId,listItemType,bgColor,itemIcon);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addLabelStyle(styleId,color,scale){
    try {
        window.frames["geIFrame"].addLabelStyle(styleId,color,scale);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addPolyStyle(styleId,color){
    try {
        window.frames["geIFrame"].addPolyStyle(styleId,color);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
function addBalloonStyle(styleId, bgColor, text){
    try {
        window.frames["geIFrame"].addBalloonStyle(styleId, bgColor, text);
    } catch (e) {
        alert('Error ['+e.message+'] occurred while opening kml ['+url+']');
    }
}
